# Changelog

本文件记录 PhilWeb Studio 的重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [1.7.0] - 2026-08-19

### Added
- **Windows 双击启动**：新增 `启动 PhilWeb Studio.cmd`，自动检查 Node.js、首次安装依赖并启动本地 Studio。
- **下载入口说明**：README 和快速上手文档新增 GitHub ZIP 下载地址与双击启动步骤。
- **正常预览模式**：Studio 右侧预览新增“正常预览 / 点哪改哪”双模式切换；正常预览可操作弹窗、按钮、主题和链接，编辑模式恢复点击定位。
- **预览定位完整性**：从正常预览停留在项目详情、联系、时间线或博客区域后，切回“点哪改哪”可直接跳回相应编辑页；点击卡片留白或装饰区也会回到该页面的默认编辑位置。
- **左下角常驻保存**：导航底部新增“保存当前页”，会按当前栏目保存基本信息、首页内容、外观、项目或博客，无需再滚到底部找按钮。
- **身份列表可视化编辑**：首页“技能 / 身份列表”改为可增删、排序、改文字并选择图标的行式编辑器；预览中每个身份图标也能直接定位到该编辑区。
- **文章卡片悬浮完整性**：首页文章卡片取消会触发滚动容器裁切的上移动画，并在列表四周预留边框空间；悬浮时顶部边框可完整显示。
- **编辑边框不再裁切**：“点哪改哪”的悬浮虚线改为向组件内部绘制；文章卡片贴近滚动区域顶部或两侧时，四条边框仍会完整显示。
- **文章标题可视化编辑**：首页“文章”标题与“我的作品”使用同一级字体和字号；点击标题可直达首页内容中的“文章板块标题”输入框。
- **自定义页面背景**：外观页新增默认主题、上传图片、上传视频三种背景内容，并可切换细网格、点阵或无图案；点击首页留白也能直接定位到背景设置。
- **点阵辨识度**：点阵背景改为跟随主题主色的双层圆点，小点构成连续纹理、间隔加入强调点，与细网格切换时有清晰可见的差异。
- **背景图案可调**：页面背景新增网格间距、点阵间距、图案颜色和图案深浅控件，修改时实时预览，保存后写入主题并在刷新和导出后继续生效。
- **首页卡片底色统一**：个人信息、首屏介绍、文章、作品、语言/主题按钮和快捷入口统一使用同一实体底色 token，背景网格或点阵不再只穿透部分卡片。

### Changed
- **全站隐藏滚动条**：Studio 编辑区、实时预览和文章分类横向列表统一隐藏浏览器滚动条，同时保留鼠标滚轮、触控板、触摸和键盘滚动能力。

## [1.6.0] - 2026-08-15

### Added
- **独立网站导出**：Studio 新增“检查并导出网站”，通过统一导出核心生成只包含静态部署文件的 ZIP；新增 `npm run export` 命令，导出包不包含 Studio、源码、模板目录或 `content/`。
- **导出前质量门槛**：导出前自动检查内容、类型、资源、构建结果和模板占位符，并在 Studio 中展示具体错误文件与字段。

### Changed
- 工具品牌统一为 **PhilWeb Studio**。
- 默认内容、职业起始内容和文档改为中性示例，导出的站点默认不带工具品牌或作者推广。
- 社交链接、邮箱、微信号和页脚署名允许为空；页脚署名默认关闭。

### Removed
- 移除固定作者 X 关注按钮、个人作者推广文案和旧模板品牌残留。

## [1.5.0] - 2026-07-13

### Added
- **数据指标可视化编辑**：Studio「首页内容」页新增「数据指标」编辑器，侧边栏底部四个小卡的标题、数值、图标都能直接改（数值留空时自动显示图标，如「AI 探索者」），保存与首屏文案同一个按钮。此前这些只能手改 `content/text/site/home.zh.md`；预览「点哪改哪」模式下点这块也会直接跳到编辑器（`home.metrics`）。服务端 `home` 接口新增 `metrics` 的定点读写（复用 `patchYamlObjectArray`）。
- **作品封面点图直传**：预览「点哪改哪」模式下，点击作品卡片左侧封面图（`project-cover:<id>`）会直接跳到该项目的「封面图」上传区并高亮；点卡片其余部分仍跳到项目表单。封面上传能力本就在「项目作品」页，这一步让它在预览里一点即达。

## [1.4.0] - 2026-07-13

### Added
- **风格包扩至 16 套**：在原有 8 套基础上新增「森野绿 / 紫棠优雅 / 暖阳琥珀 / 深海靛蓝 / 大地摩卡 / 石墨极简 / 晴空青蓝 / 樱绯书卷」，覆盖绿/紫/琥珀/深海/大地/石墨/青蓝/樱绯等此前缺失的色系，全部复用现有字体预设、零网络依赖。
- **职业起始人设模板**：`content:reset` 支持 `--template <id>` 覆盖式叠加与 `--list` 列出可选职业起点，一条命令即可替换首页文案、时间线、简历和技能矩阵。人设存于 `templates/personas/<id>/`，只存差异化文本和推荐风格包。
- **卡片色调浓度可调**：单卡片配色新增「色调浓度」滑块（20%–90%，默认 40%），控制五张卡片色调叠加的透明度——越高越接近所选纯色、越低越通透文字越清晰。持久化于 `effects.cardTintOpacity`。
- **卡片内部元素跟随圆角**：侧栏指标块、技能/工具图标底、作品列表行的圆角改为跟随「卡片圆角」滑块（复用 `--card-radius-sm`），选粗野 0px 内部也变方，选圆润主题内部也跟着圆；时间线的圆形节点保持圆形。
- **液态玻璃质感开关**：外观页新增「液态玻璃质感」开关（`effects.cards.glassState`），关闭后首页五张卡片回落成干净的实心卡（去掉背景模糊、光晕与镜面）。风格包各自携带该开关——「新粗野主义 / 包豪斯 / 瑞士国际主义 / 石墨极简」默认关闭玻璃，其余默认开启，进一步拉开风格差异。实时预览与保存后的站点一致（`App.tsx` 与 `edit-overlay.js` 均按 `--theme-effects-cards-glass-state` 切换根元素的 `no-glass` 类）。

### Changed
- **Studio 外观页改为"模板优先"**：风格包画廊置顶并常驻"应用外观设置"按钮，选一个即成型；主题色、圆角、光晕、疏密、阴影、浮动、单卡片配色等逐项微调统一收进"高级微调（可选）"折叠区，默认收起，降低非设计师用户的定制成本。
- **风格包携带五卡片色调**：每个风格包现在各自带一套首页五卡片（个人资料/问候/时间线/作品/快捷入口）的协调色调，套用风格包即整体换色，区别度大幅提升；单卡片配色仍可在高级微调里手动调整，保存为自定义方案时一并记录。
- **示例图片改为中性占位图**：头像、项目封面/截图、博客封面统一换成白/灰中性占位图（不挑明暗主题、不干扰整体观感），用户换成自己的图即可。
- 文档（README / docs/CUSTOMIZE.md / customize-site 技能）同步更新风格包数量与职业人设用法。

### Added（示例内容）
- **示例作品默认 3 个**：新增示例项目「字体搭配 TypePair」（排版方向，紫色主题），首页"我的作品"默认展示 3 张卡（原 2 张）；同步更新示例简历的精选作品与侧栏"独立产品"指标（2 → 3）。项目列表本就无数量上限，按 `meta.yml` 的 `order` 排序。

### Fixed
- **修复液态玻璃卡片的色调不生效**：首页五张卡片用的是液态玻璃变体（`.liquid-card`），其 `background: var(--liquid-surface) !important` 一直覆盖着卡片的 `--card-tint` 图层，导致"单卡片配色"与"风格包色调"在这些卡片上完全看不出来（自 v1.2.0 起就存在）。现将 `--card-tint` 叠加到液态表面之上，默认色调、单卡片手调、风格包切换现在都会真实改变卡片颜色。

### Removed
- **移除 Studio「发布上线」标签页**：删除页内"体检 / 打包 / 打开 dist 文件夹"按钮与配套服务端接口（`/studio/api/deploy/*`）、构建进程与进度清单里的"发布"项。发布改为终端 `npm run build` + 拖拽 dist 上传；操作说明与页脚署名开关移至 Studio 帮助页。

## [1.3.0] - 2026-07-12

### Removed
- **整体移除个人 AI 对话功能**：删除右下角 AI 卡片、Coze SDK 接入（`data/cozeChatbot.ts`）、服务端 token 路由（`api/`、`functions/`、`server/`）、相关主题样式与内容字段（`quickLinks.personalAI*`、`assets.talkAvatar`、`features.personalAI`）、Studio 开关与全部文档提及；`jose` 依赖随之移除。构建产物减小约 24KB JS + 7KB CSS。

## [1.2.0] - 2026-07-12

### Added
- **卡片浮动动画开关**：`effects.cards.floatState`，Studio 外观页一键开关，实时预览。
- **阴影风格**：柔和投影 / 硬边色块（新粗野主义）/ 无阴影三档，同时作用于普通卡片与液态玻璃卡片。
- **风格包扩充**：新增「包豪斯」「瑞士国际主义」，「新粗野主义」升级为硬边阴影 + 关浮动；风格包现在携带阴影与浮动设置。
- **自定义方案**：把当前整套外观设置保存为命名方案（存于 `content/theme/custom-packs.json`），一键切回、可删除。
- **单卡片配色**：首页五张卡片（个人资料/问候/时间线/作品/快捷入口）可分别叠加半透明色调，留空跟随主题。

## [1.1.0] - 2026-07-12

### Added
- **时间线可视化编辑**：Studio 首页内容 tab 直接编辑成长轨迹——图标选择器（24 个精选 Material Symbols）、时期/标题/关键词/描述文本框、增删与排序。
- **可配置个人 Logo**：首屏 Logo 改为读取 `shared.yml` 的 brandMark，可在 Studio 上传或移除，默认隐藏。
- **个人 AI 功能开关**：`content/config/site.yml` 新增 `features.personalAI`（默认关闭），未配置 Coze 时右下角卡片整体隐藏。

### Changed
- 定制器界面翻新：内嵌 Lucide 风格 SVG 图标替换全部 emoji，保存按钮加载状态，文案与操作逻辑梳理。
- 项目卡顺序与主题色完全数据驱动（meta.yml 的 order 与 themeColor）。

## [1.0.0] - 2026-07-11

### Added
- 首个公开版本：瑞士设计风格个人作品集模板（React 19 + Vite + Tailwind，中英双语、深色模式、内容与代码分离）。
- **Studio 可视化定制器**（`npm run studio`，仅本地开发存在）：点哪改哪、定制进度清单、6 套整站风格包实时预览、基本信息与图片上传（自动压缩）、项目与博客管理、发布页（体检/打包/拖拽部署指引）、撤销与重置。
- **中性示例内容包**：`npm run content:reset -- --yes` 一键回到适合继续填写的通用示例内容。
- **Claude Code 定制技能**：`.claude/skills/customize-site/`，支持用自然语言让 Claude 修改网站内容。
- **页脚署名开关**：`content/config/site.yml`，默认关闭，可在 Studio 帮助页按需开启。
