# PhilWeb Studio 快速上手

## 1. 打开编辑器

直接打开：

<https://fanzr-arch.github.io/philweb-studio/>

不需要安装 Node.js，不需要 GitHub 登录，也不需要后端。

首次进入会加载一份中性示例网站。如果你以前在这个浏览器里编辑过，会自动恢复上次的项目，不会覆盖。

## 2. 编辑

- 左侧改内容，右侧立即预览。
- 电脑 / 手机预览切换不会刷新预览页。
- 「正常预览」按真实网站操作；「点哪改哪」点击预览内容会跳到对应表单。
- 中英文要成对填写；切换语言前会自动保存。
- 身份列表可以添加、选图标、排序和删除。

保存方式：

- 表单改动约 0.5 秒后自动保存
- 左下角「保存当前页」在任意滚动位置都可见
- `Ctrl+S` / `Cmd+S` 立即写入

状态会显示：未保存 / 保存中 / 已保存 / 保存失败。

## 3. 数据保存在哪

只保存在**当前浏览器**。

- 刷新、关掉标签再打开，数据还在
- 换电脑、换浏览器、无痕窗口不会同步
- 清浏览器数据前必须先导出工程备份

## 4. 导出

**网站 ZIP**：给访客看的成品，解压后上传到 Netlify、Cloudflare Pages 或 Vercel。

**工程备份**：给你自己继续改。换设备前导出，到新浏览器里选择「导入为新项目」（推荐）或覆盖当前项目。

导出前会检查姓名、模板占位符、图片引用和重复 ID。检查失败时会列出字段，不会生成残缺 ZIP。

## 5. 本地开发者

如果要改工具本身：

```bash
npm install
npm run studio
```

Windows 也可以双击 `启动 PhilWeb Studio.cmd`。
