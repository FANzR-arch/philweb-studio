# PhilWeb Studio 快速上手

## 1. 下载与启动

Windows 用户下载并解压项目后，可以直接双击根目录里的 `启动 PhilWeb Studio.cmd`。首次启动会自动安装依赖，之后会自动打开本地 Studio。

如果使用 GitHub，项目地址是：

<https://github.com/FANzR-arch/philweb-studio>

直接下载 ZIP：

<https://github.com/FANzR-arch/philweb-studio/archive/refs/heads/main.zip>

首次运行前需要安装 [Node.js LTS](https://nodejs.org/)，并保持网络连接。

## 2. 手动安装

安装 Node.js LTS，然后在项目文件夹打开终端：

```bash
npm install
```

## 3. 打开编辑器

```bash
npm run studio
```

浏览器会打开 `http://localhost:3000/studio`。

右侧预览栏提供两种模式：

- **正常预览**：按真实网站方式点击弹窗、按钮、主题和链接。
- **点哪改哪**：点击预览中的文字或图片，直接定位到左侧对应编辑位置。

推荐顺序：

1. 基本信息：联系方式、头像、二维码和 Logo。
2. 首页内容：名字、介绍、技能、时间线和指标，中英文分别填写。
3. 外观风格：选择风格包，再按需微调。
4. 项目作品：替换示例项目、上传封面和详情图，或新建项目。
5. 博客文章：不需要博客时可以全部下架或清空。
6. 检查并导出：下载独立网站 ZIP。

## 4. 导出并部署

点击 Studio 顶部或帮助页的“检查并导出网站”。检查通过后会下载 ZIP。

解压 ZIP 后，把其中的文件上传到：

- Netlify Drop
- Cloudflare Pages
- Vercel
- 任何支持静态 HTML 的托管服务

导出包是最终网站，不包含编辑器和模板源码。需要修改时，回到原来的 PhilWeb Studio 项目重新导出。

## 5. 内容规则

- 中英文首页、简历和项目内容要成对维护。
- 图片放在 `content/media/`，不要直接放到构建目录。
- 不要手动修改 `data/content.generated.ts` 和 `data/imageManifest.ts`。
- 导出前必须处理示例姓名、假邮箱和社交账号占位符。

## 6. 手动命令

```bash
npm run content:check  # 内容校验
npm run verify         # 同步内容、图片、类型和资源预算
npm run build          # 生成普通 dist 构建
npm run export         # 生成独立网站 ZIP
```
